# ADR 003 - QR Over Rotation

## Decision

Implement QR factorization as the main matrix operation.

## Rationale

The statement mentions matrix rotation ambiguously, but it explicitly requires QR factorization, which is more precise and testable.

## Consequence

The ambiguity is documented instead of adding a second interpretation that would increase scope.
